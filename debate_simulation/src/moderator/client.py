"""LLM client for the moderator.

Wraps the model call, JSON parsing into `ModerationResponse`, retries, and
capture of call metadata (model id, tokens, latency).

Placeholder — implemented in a later step.
"""
