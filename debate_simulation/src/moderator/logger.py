"""Incremental persistence of moderation records.

Writes one `ModerationRecord` per evaluated candidate under `experiments/`,
appended as it happens so a run can be resumed at turn level after a failure.

Placeholder — implemented in a later step.
"""
